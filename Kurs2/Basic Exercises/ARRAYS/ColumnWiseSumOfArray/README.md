6. Column-wise Sum of a 2D Array
Write a program that:

Declares a 2D array of size 3x3.
Prompts the user to input values into the array.
Computes and prints the sum of elements of each column.
Hint: This is similar to row-wise sum, but instead of iterating over rows, you iterate over columns.
